# name: test/sql/redduck.test
# description: test redduck extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT redduck('Sam');
----
Catalog Error: Scalar Function with name redduck does not exist!

# Require statement will ensure this test is run with this extension loaded
require redduck

# Confirm the extension works
query I
SELECT redduck('Sam');
----
Redduck Sam 🐥

query I
SELECT redduck_openssl_version('Michael') ILIKE 'Redduck Michael, my linked OpenSSL version is OpenSSL%';
----
true
